# 接下來

> 順序是有理由的，理由在 `architecture.md`。

## 現在的狀態（2026-09-17）

已經能用的：

- 完整行程、費用、待辦（共同＋個人）、自訂行程
- 跟隊模式：全螢幕單卡、七種狀態、倒數指向真死線
- `?mode=follow` 連結 —— 傳給同行的人，打開就是跟隊畫面
- 每列行程可以「看跟隊畫面」（任意時刻預覽）和「寫跟隊內容」（帶隊的人自己編輯）
- 亮／暗主題、離線可用、資料驗證進 CI

還不能用的：

- **帶隊的人寫的東西傳不出去**（只存在自己手機）
- **內容要人寫**，或用 AI 起草再由人確認（不會自動生成）
- 一次只能一趟旅行

## 馬來西亞行前（10/08 之前）

這一段的目標是**測產品**，不是把行程做完。

- [ ] 用「看跟隊畫面」把 19 個時刻逐一走過，找出講不清楚的地方
- [ ] 三個空檔日（10/09、10/11、10/12）要不要填內容 —— **需要使用者決定**
- [ ] 把連結發給另外四個人，觀察他們第一次打開時做了什麼
- [ ] 六個推導出來的時刻（入境、退房、抵達等）值得手寫成真的內容

## 階段 A：AI 生成內容

**為什麼排第一**：價值最高、不需要後端、而且會讓「一份新 `trip.js`」從半天變成幾分鐘。

- [x] 「複製 prompt 出去、貼 JSON 回來」—— 整趟一次，或單一個項目（D13）
- [x] 生成結果過跟人一樣的閘門，違規整筆不套用並說明原因（D14）
- [x] 標記 `by:'ai'`，人按下儲存才會消失（D15）
- [x] 套用可以一次復原，也可以清掉所有沒檢查過的草稿，所以能反覆重抽（D16）
- [x] **第一次真實測試（免費 GPT）**：格式 5/5 全過閘門，內容 3/5 的 `line` 是錯的（動作不見了、講成下一段、自己加指令），`need` 五筆全是雜訊，`fallback` 一筆是贅述且蓋掉資料裡真正的備案
- [x] 依據上面的失敗改 prompt，並加上贅述警告（D17、D18）
- [ ] **再測一次，看那幾條規則是否真的擋住了** ← 現在卡在這
      - 特別看：`line` 還會不會講成下一段、`need` 是不是真的留空了、`fallback` 是不是從備註來的
- [ ] 夠好的話再考慮自動化（取捨表在 `architecture.md`；BYOK 只在自己用時可接受）

## 階段 C：多行程

- [ ] 行程清單，`trip.js` 從打包檔變成可載入的資料
- [ ] 解決 `manifest.webmanifest` 寫死旅行名稱的問題

## 階段 B：後端

**條件沒滿足就不要開始。** 條件寫在 `architecture.md` B 段。

## 未決的問題

1. **AI 生成的內容要不要讓使用者看到 prompt？** 關係到信任 —— 看得到就知道它憑什麼這樣寫。
2. **推導卡片要陽春到什麼程度？** 太陽春沒用，太完整就沒有人想寫真的內容。
3. **通知。** 「該走了」需要人主動打開 app 才看得到，這違反 HUD 的前提。Web Push 需要後端，排在 B 之後；在那之前這是這個產品的天花板，**要誠實承認**。
4. **出事的時候怎麼辦。** 班機延誤、有人走丟 —— 現在整個系統假設計畫不會變。
